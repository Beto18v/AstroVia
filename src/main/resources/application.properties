spring.application.name=astrovia

# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/astrovia_db
spring.datasource.username=postgres
spring.datasource.password=Nicolas0318-
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Desactiva cach� para ver cambios sin reiniciar
spring.thymeleaf.cache=false

# Ubicaci�n y extensi�n por defecto
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html

# (Opcional) Modo de plantillas
spring.thymeleaf.mode=HTML

logging.level.org.thymeleaf=TRACE

# ================== JWT ==================
# Clave base64 (usar una de >=32 bytes antes de base64). Cambiar en producción.
app.jwt.secret=ZmFrZVNlY3JldEtleUZvckFzdHJvVmlhMTIzNDU2Nzg5MDEyMzQ=
app.jwt.expiration-seconds=3600
app.jwt.refresh-expiration-seconds=86400

# ================== CORS =================
# Orígenes permitidos separados por coma. Ej: http://localhost:4200,https://app.astrovia.com
app.cors.allowed-origins=http://localhost:4200
